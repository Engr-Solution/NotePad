import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  container: {
    marginTop: 20,
    boxShadow: "initial",
    margin: "0 auto",
    padding: "0 10px",
  },
}));

export default useStyles;
